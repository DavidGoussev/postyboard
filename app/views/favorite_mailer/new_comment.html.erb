<!DOCTYPE html>

<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <h1>guess what, there's a new comment to "<%= @post.title %>"</h1>
    <small>
      by: <%=@comment.user.name %>
    </small>
    <p>
      <%= @comment.body %>
    </p>
    <p>
      <%= link_to "enjoy it in full, view comment on site", topic_post_url(@post.topic, @post, anchor: "comment-#{@comment.id}") %>
    </p>
  </body>
</html>
